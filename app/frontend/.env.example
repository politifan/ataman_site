VITE_API_BASE=http://localhost:8000
# Production builds use the current site origin automatically.
# Leave VITE_API_BASE unset in production.
# Optional legacy token fallback (recommended to keep empty when using login/password)
VITE_ADMIN_TOKEN=
